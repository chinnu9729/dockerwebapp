---
version: "3.9"

services:
  devopsdb:
    image: shaikmustafa/mynewapp:dbimage
    ports:
      - "3306:3306"
    deploy:
      replicas: 1
    networks:
      - mynetwork

  application:
    image: shaikmustafa/mynewapp:appimage
    ports:
      - "1111:8080"
    deploy:
      replicas: 3
    depends_on:
      - devopsdb
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: overlay
